{"BEFORE":"        if isinstance(labels, str):\n            labels = [labels]\n        return self.model(\n","AFTER":"        labels = upgrade_to_sequence(labels)\n        labels = list(map(str, labels))\n"}