{"BEFORE":"        return (ssimLoss, l1l2Loss, sum(mlmLoss) \/ len(self._mLoss) + 0.1 * reg), (restored, codes, None, logits, None)\n","AFTER":"        return (ssimLoss, l1l2Loss, sum(mlmLoss) \/ len(self._mLoss)), (restored, codes, None, logits, None)\n"}