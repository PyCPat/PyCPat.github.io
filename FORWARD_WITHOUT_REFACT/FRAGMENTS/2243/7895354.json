{"BEFORE":"        return torch.cat((shifted, is_pad), dim=-1)\n","AFTER":"        mask = torch.cat((shifted, is_pad), dim=-1)\n\n        # Expand the tensor.\n        return mask.expand(x.shape + mask.shape[-1:])\n"}