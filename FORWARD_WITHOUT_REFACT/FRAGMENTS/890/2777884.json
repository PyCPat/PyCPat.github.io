{"BEFORE":"        x = input_list[\"foo\"] if input_list else torch.ones(3)\n","AFTER":"        device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n        x = input_list[\"foo\"] if input_list else torch.ones(3).to(device)\n"}