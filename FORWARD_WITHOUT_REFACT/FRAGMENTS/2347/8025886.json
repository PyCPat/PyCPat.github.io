{"BEFORE":"        y = self.encoder(x)\n        f = self.feature_maker(y)\n        y_ = self.feature_demaker(f)\n        x_ = self.decoder(y_, self.encoder.pool_indeces)\n\n        return x_\n","AFTER":"        return self.decode(self.encode(x))\n"}