{"BEFORE":"        out = self.stem(x)\n\n        return out\n","AFTER":"        out = self.stem(x)\n        out = self.body(out)\n        out = self.activation(self.final_conv(out))\n\n        pool = torch.mean(out, dim=(2,3))\n\n        if self.training:\n            pool = self.dropout(pool)\n\n        return self.fc(pool)\n"}