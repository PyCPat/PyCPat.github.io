{"BEFORE":"        g.update_all(fn.copy_u('h', 'm'), fn.sum('m', 'h'))\n","AFTER":"            edge_weight = g.edata.get(_EDGE_WEIGHT, edge_weight)\n\n        feat_list = []\n        for conv in self.conv:\n            feat = conv(g, feat, edge_weight=edge_weight)\n            feat_list.append(feat)\n\n        g = g.local_var()\n\n        g.ndata['h'] = self.jump(feat_list)\n        if edge_weight is not None:\n            g.edata['_edge_weight'] = edge_weight\n            g.update_all(fn.u_mul_e('h', '_edge_weight', 'm'),\n                         fn.sum('m', 'h'))\n        else:\n            g.update_all(fn.copy_u('h', 'm'), fn.sum('m', 'h'))\n\n        return self.mlp(g.ndata['h'])\n"}