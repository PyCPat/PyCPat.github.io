{"BEFORE":"        batch_size = fv.shape[0]\n        seq_len = fi.shape[1]\n\n        if fv_alter is None:\n            v_in = fv\n        else:\n            v_in = fv * dec[:, :, 0].unsqueeze(-1) + fv_alter * dec[:, :, 1].unsqueeze(-1)\n\n        fused = self.fuse(v_in, fi)\n","AFTER":"        v_in = fv * dec[:, :, :1] + fv_alter * dec[:, :, -1:] if fv_alter is not None else fv\n"}