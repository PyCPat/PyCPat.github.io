{"BEFORE":"        return self.scale(loss \/ divisor, unet_number = unet_number)\n","AFTER":"            scaled_loss = self.scale(loss \/ divisor, unet_number = unet_number)\n            scaled_loss.backward()\n        return loss.item()\n"}