{"BEFORE":"            return (pred * weight.reshape(weight.shape[0], -1)).sum()  # 0\n","AFTER":"            if pred.dim() == weight.dim() + 1:\n                weight = weight.unsqueeze(1)\n            return (pred * weight).sum()  # 0\n"}