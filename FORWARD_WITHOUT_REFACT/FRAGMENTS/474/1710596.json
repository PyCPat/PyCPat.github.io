{"BEFORE":"        x = x + self.pos_emb(torch.arange(n, device = device))\n","AFTER":"    def forward(self, x, mask = None):\n        n, device = x.shape[1], x.device\n        pos_emb = self.pos_emb(torch.arange(n, device = device))\n\n        x = x + pos_emb\n"}