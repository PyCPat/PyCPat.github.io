{"BEFORE":"        self.resid = residual_block(64, 64, 1)\n","AFTER":"        self.num = FLAGS.num_resblock\n        self.resids = nn.ModuleList([residual_block(64, 64, 1) for i in range(self.num)])\n"}