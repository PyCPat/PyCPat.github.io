{"BEFORE":"        out = self.classifier(x)\n        return out.flatten(1)\n","AFTER":"        x = self.classifier(x)\n        if not self.global_pool.is_identity():\n            x = x.flatten(1)  # conv classifier, flatten if pooling isn't pass-through (disabled)\n        return x\n"}