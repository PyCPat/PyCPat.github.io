{"BEFORE":"        x = self.proj(x).flatten(2).transpose(1, 2)\n","AFTER":"        x = self.proj(x)\n        if self.flatten:\n            x = x.flatten(2).transpose(1, 2)  # BCHW -> BNC\n        x = self.norm(x)\n"}