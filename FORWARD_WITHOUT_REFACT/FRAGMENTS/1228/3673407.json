{"BEFORE":"        skips = None\n        for conv in self.conv_layers:\n            x, h = conv(x)\n            if skips is None:\n                skips = h[..., self.l_diff:-self.l_diff]\n            else:\n                skips += h[..., self.l_diff:-self.l_diff]\n\n        x = skips\n","AFTER":"        skips = 0\n        for conv in self.conv_layers:\n            x, h = conv(x)\n            skips += h\n\n        skips *= math.sqrt(1.0 \/ len(self.conv_layers))\n"}