{"BEFORE":"        classCounts = self.counts[instance.attrs[index]]\n        max, maxClass = 0, None\n        classValues = classCounts.keys()\n        for classValue in classValues:\n            count = classCounts[classValue]\n            if count > max:\n                max = count\n                maxClass = classValue\n        return maxClass\n","AFTER":"        return self.maxCounts[instance.valueAt(self.index)].klassValue\n"}