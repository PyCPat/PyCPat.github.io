{"BEFORE":"        self.LayerNorm = nn.LayerNorm(config.hidden_size, eps=config.layer_norm_eps)\n","AFTER":"        self.LayerNorm = nn.LayerNorm(config.hidden_size, eps=config.layer_norm_eps) if config.norm_type==\"layer_norm\" else Norm(eps=config.layer_norm_eps)\n"}