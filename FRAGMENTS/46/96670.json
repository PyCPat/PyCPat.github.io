{"BEFORE":"        var = self.token()\n        assert re.match('^[exps]\\d+$', var), var\n        n = {'e': 0, 'x': 1, 'p': 2, 's': 3}[var[0]]\n        return int(str(n) + var[1:]) # for topic(e1,p1), as per boxer revision 2554\n","AFTER":"        var = self.token()\n        assert re.match('^[exps]\\d+$', var), var\n        return var\n"}