{"BEFORE":"        temp = self.tag_sents([tokens])\n        # Handle the case where return more than one sentence. \n        # Note that if it is the case, user should use tag_sents instead  \n        result = []\n        for sent in temp:\n            result += sent\n        return result \n","AFTER":"        return list(self.tag_sents([tokens]))\n"}