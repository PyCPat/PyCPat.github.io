{"BEFORE":"        self.fc = nn.Linear(filters * 32, num_classes)\n","AFTER":"        self.features = self.make_layers(filters)\n\n        self.avg = nn.AdaptiveAvgPool2d((1, 1))\n        self.classifier = nn.Sequential(\n            nn.Dropout(0.2),\n            nn.Linear(filters * 32, num_classes)\n        )\n"}