{"BEFORE":"            return Tree.fromstring(self._normalize(t))\n","AFTER":"            tree = Tree.fromstring(self._normalize(t))\n            # If there's an empty node at the top, strip it off\n            if tree.label() == '' and len(tree) == 1:\n                return tree[0]\n            else:\n                return tree\n\n        except ValueError as e:\n"}