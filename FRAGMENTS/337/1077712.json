{"BEFORE":"        luinfo['semType'] = AttrDict()\n        list(\n            map(luinfo.pop, [k for k in list(luinfo.keys()) if k in ignorekeys]))\n","AFTER":"        luinfo['semType'] = AttrDict()\n        for k in ignorekeys:\n            if k in luinfo:\n                del luinfo[k]\n\n        for sub in elt:\n"}