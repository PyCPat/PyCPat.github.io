{"BEFORE":"        if nonlinear == 'relu':\n            self.nonlinear = nn.ReLU()\n        elif nonlinear == 'sigmoid':\n            self.nonlinear = nn.Sigmoid()\n        else:\n            raise NotImplementedError()\n            \n    def forward(self, input, skip=None):\n","AFTER":"        self.nonlinear = choose_nonlinear(nonlinear)\n"}