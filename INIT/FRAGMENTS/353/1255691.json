{"BEFORE":"    self.h = nn.Sequential(nn.Linear(state_size, hidden_size), nn.Tanh(), nn.Linear(hidden_size, hidden_size), nn.Tanh(), nn.Linear(hidden_size, 1))  # Shaping function Φ\r\n","AFTER":"    self.h = _create_fcnn(state_size, hidden_size, 1, 'tanh')  # Shaping function Φ\r\n"}