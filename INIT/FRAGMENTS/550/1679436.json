{"BEFORE":"            nn.LSTMCell(\n                in_size,\n                hidden_size,\n            ).to(device)\n            for in_size in [input_size] + [hidden_size] * (num_layers - 1)\n        ]\n","AFTER":"        self.lstm_layers = nn.ModuleList(\n            [\n                nn.LSTMCell(\n                    in_size,\n                    hidden_size,\n                ).to(device)\n                for in_size in [input_size] + [hidden_size] * (num_layers - 1)\n            ]\n        )\n"}