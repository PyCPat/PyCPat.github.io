{"BEFORE":"        raise NotImplementedError(\"Implement D3Net\")\n","AFTER":"        x = input\n        stacked = []\n\n        stacked.append(input)\n\n        for idx in range(self.num_d3blocks):\n            if idx != 0:\n                x = torch.cat(stacked, dim=1)\n            x = self.net[idx](x)\n            stacked.append(x)\n        \n        output = torch.cat(stacked[1:], dim=1)\n\n        return output\n"}