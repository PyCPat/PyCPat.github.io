{"BEFORE":"        y = self.backbone(x) + x\n","AFTER":"        x = self.quant(x)\n        y = self.shortcut.add(self.backbone(x), x.repeat(1, self.colors*self.scale*self.scale, 1, 1).contiguous())\n        y = self.upsampler(y)\n        y = self.dequant(y)\n"}