{"BEFORE":"        for conv, pool in zip(self.conv, self.pool):\n            x = self.dropout(F.leaky_relu(pool(conv(x)))) # (B, num_filters, N)\n        return x\n","AFTER":"        return self.net(x)\n"}