{"BEFORE":"            out = layer(out)\n        return state + out\n","AFTER":"            out = layer(out)\n\n        mean = out[:, :self.output_dim]\n\n        logvar = self.max_logvar - F.softplus(self.max_logvar - out[:, self.output_dim:])\n        logvar = self.min_logvar + F.softplus(logvar - self.min_logvar)\n        return mean, torch.exp(logvar)\n"}