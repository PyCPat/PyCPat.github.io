{"BEFORE":"        output = self.fc(feature.view(img.shape[0], -1))\n        return output\n","AFTER":"        x = self.features(x)\n        x = x.view(x.size(0), 256 * 2 * 2)\n        x = self.classifier(x)\n        return x\n"}