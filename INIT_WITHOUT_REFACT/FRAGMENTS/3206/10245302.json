{"BEFORE":"        self.l1.flatten_parameters()\n\n        a, h = self.l1(state, hidden)\n","AFTER":"        if self.recurrent:\n            self.l1.flatten_parameters()\n            a, h = self.l1(state, hidden)\n        else:\n            a, h = self.l1(state), None\n\n        a = self.l2(a)\n"}