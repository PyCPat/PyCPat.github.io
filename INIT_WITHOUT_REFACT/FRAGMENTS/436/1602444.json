{"BEFORE":"    def forward(self, embed, embed_len) -> Tuple[torch.Tensor, torch.Tensor]:\n        x = torch.transpose(embed, 1, 2)\n        x = self.layers(x)\n        x = torch.transpose(x, 1, 2)\n        return x, torch.div(embed_len + 1, 2, rounding_mode='floor')\n","AFTER":"        return self.layers(embed)\n"}