{"BEFORE":"        x = F.tanh(self.fc2(x))\n        mu =  F.tanh(self.pi(x))\n        sigma = F.softplus(self.sigma(x)) +1e-3\n        return mu,sigma\n","AFTER":"        x = F.tanh(self.fc2(x))\n        mu = self.pi(x)\n        logstd = torch.zeros_like(mu)\n        std = torch.exp(logstd)\n        return mu,std\n"}