{"BEFORE":"        return output, hidden\n","AFTER":"        hidden = torch.tanh(self.f_concat_h(torch.cat((hidden[-2], hidden[-1]), dim=1))) # (bs, H)\n\n        return output, hidden.squeeze(0) # (T, bs, 2H), (bs, H)\n"}