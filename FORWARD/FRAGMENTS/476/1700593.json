{"BEFORE":"        x = torch.cat([x, x], dim = -1)\n        x = self.layers(x, **kwargs)\n        return torch.stack(x.chunk(2, dim=-1)).mean(dim=0)\n","AFTER":"        return self.layers(x, **kwargs)\n"}