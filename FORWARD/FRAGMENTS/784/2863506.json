{"BEFORE":"        return torch.cos(projected + self.bias) * torch.sin(projected)\n","AFTER":"        output = torch.cos(projected + self.bias) * torch.sin(projected)\n        return output.as_subclass(MAP)\n"}