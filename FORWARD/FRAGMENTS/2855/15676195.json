{"BEFORE":"        probs = torch.sigmoid(chosen_reward - reject_reward)\n        log_probs = torch.log(probs)\n        loss = -log_probs.mean()\n","AFTER":"        loss = torch.log(1 + torch.exp(reject_reward - chosen_reward)).mean()\n"}