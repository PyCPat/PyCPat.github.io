{"BEFORE":"        x = torch.cat([x, c], dim=1)\n        return self.main(x)\n","AFTER":"        x = torch.cat([x, c], dim=1)\n\n        feature_maps = []\n\n        # Get intermediate feature maps\n        for layer in self.main:\n            x = layer(x)\n            feature_maps.append(x)\n\n        return x, feature_maps\n"}