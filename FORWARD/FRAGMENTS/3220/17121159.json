{"BEFORE":"        return self.model(state)\n","AFTER":"        state = self.model.forward(state)\n        state = nn.ReLU()(state)\n        if self.sac:\n            mean = self.fc_mean(state)\n            log_std = self.fc_std(state)\n            log_std = torch.clamp(log_std, min=-20.0, max=2.0)\n            return mean, log_std\n\n        return state\n"}