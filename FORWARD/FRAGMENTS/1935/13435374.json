{"BEFORE":"        return ZeroConvFunc.apply(torch.zeros([x.size(0), self.out_dim]).to(x.device))\n","AFTER":"        out = torch.zeros_like(x)\n        out.requires_grad = True\n        return out\n"}